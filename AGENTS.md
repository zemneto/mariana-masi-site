<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# Handoff e organização

Leia `HANDOFF.md` (raiz deste repo) por completo antes de mexer em qualquer coisa — é o estado atual do projeto, incluindo pendências ativas (tipografia, tratamento de cor/fotos, conteúdo `[REVISAR]`). Este repositório faz parte do `~/Desktop/Ecosystem`; veja `~/Desktop/Ecosystem/AGENTS.md` para o mapa geral.

**Não confundir com Instituto Nexium Site nem Nexium Clinic** — domínio (`marianamasi.com`), propósito e identidade visual completamente diferentes. Mesma conta Vercel só, quando publicado.

**Nunca inventar credenciais, biografia ou formação da Mariana.** Onde a informação real não existir, usar um placeholder `[REVISAR]` claramente marcado (ver padrão em `src/content/profile.ts` e `messages/*.json`) em vez de preencher com suposições.

Ao terminar um bloco de trabalho relevante, **atualize `HANDOFF.md` diretamente** — não crie um novo arquivo de status datado.
